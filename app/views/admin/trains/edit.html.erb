<div class="row">
  <div class="col-md-4">
    <h1><%= t('.title') %></h1>

    <%= render 'form', train: @train %>

    <%= link_to t('common.show'), [:admin, @train] %> |
    <%= link_to t('common.back'), admin_trains_path %>
  </div>
</div>
