<%= form_with(model: route, local: true) do |form| %>

  <%= render 'errors', route: route %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
